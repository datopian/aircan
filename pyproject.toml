[project]
name = "aircan-constraint-matrix"
version = "0.1.0"
description = "End-to-end CKAN, Aircan, and datastore constraint tests"
readme = "CONSTRAINT_TESTS.md"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.32,<3",
]

[tool.uv]
package = false
